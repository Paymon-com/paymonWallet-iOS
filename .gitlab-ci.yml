stages:
  - build

build_project:
  stage: build
  script:
    - xcodebuild clean -workspace paymon.xcworkspace -scheme paymon | xcpretty
    - xcodebuild test -workspace paymon.xcworkspace -scheme paymon -destination 'platform=iOS Simulator,name=iPhone 8,OS=12.1' | xcpretty -s
  tags:
    - ios_9-2
    - xcode_7-2
    - osx_10-11